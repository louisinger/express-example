const app = require("./app");

// Plus tous les bails server ici genre port etc 
// + importe l'app express déclaré dans app.js

app.listen("3000", function() {
  console.log("Listening on port 3000");
});
